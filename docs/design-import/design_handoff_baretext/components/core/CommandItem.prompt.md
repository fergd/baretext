CommandItem — one row in the ⌘K palette. Active state is an inset 3px accent stripe + wash (never a left border, never shifts the label).

\`\`\`jsx
<CommandItem icon="run" label="Start sprint" keys={['⌘','⇧','S']} active />
<CommandItem icon="run" label="Switch to Sprinter" checked />
\`\`\`
