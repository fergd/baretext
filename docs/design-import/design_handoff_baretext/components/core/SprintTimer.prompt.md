SprintTimer — Sprinter's distraction-light timer. Full = glass panel (time, progress, word delta). \`minimized\` = just the runner glyph + "Sprinting" for the status bar.

\`\`\`jsx
<SprintTimer time="14:32" progress={42} words={180} goal={500} />
<SprintTimer minimized />
\`\`\`
